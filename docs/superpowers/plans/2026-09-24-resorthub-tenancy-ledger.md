# SDD ledger — plan: docs/superpowers/plans/2026-09-24-resorthub-tenancy.md

Spec: docs/superpowers/specs/2026-09-24-resorthub-tenancy-design.md
Worktree: /Users/wiljans/vsp/pasala_resorthub (branch feat/resorthub-tenancy, from main ee0e784)
Local DB backup (data, public+auth): /Users/wiljans/vsp/pasala-db-backups/local-before-resorthub-20260924-1722.sql

## Preflight scan

| Pair / task | Shared file or interface | Finding |
|---|---|---|
| T1/T2/T3 | 0043, test 36 plan counts 12→17→18 | consistent |
| T2 vs T5–T9 | "no link" tables (coupons, outbox, staff_shifts, leave_requests, attendance_records, tasks) set NOT NULL in T2 | CONFLICT: existing triggers (enqueue_reservation_outbox) and existing tests insert without property_id until T6/T9 rewrite them; suite would break at T2 |
| T4 vs T5 | memberships before policies | consistent |
| T5 vs existing tests | authenticated test inserts into ops tables without property_id fail once RLS requires W(property_id) | T5 must add property_id to those inserts (plan only mentions memberships) |
| T5 vs T9 | service_requests/maintenance/tasks write policy + trigger rewrite | consistent (policy T5, trigger T9) |
| T6–T10 | 0045, test 37 plan counts +4/+4/+3/+4 | consistent; appended blocks set resort status in sequence |
| T10 vs T12 | test 38 uses profiles.platform_role; T12 renames | T12 text covers it |
| T11 vs T12 | T11 definer allow-list omits legacy helpers still present until T12 | CONFLICT: guard would fail at T11 |
| T11 guard 2 | reviews_read_all `true` | intended |
| T13 vs T14 | legacy UserRole kept in T13, removed in T14 | consistent |
| T14 vs T18 | T14 deletes UserRole; users_screen.dart + user_admin_repository.dart use it and are only deleted in T18 | CONFLICT: app would not compile after T14 |
| T15–T17 | repository signatures, model toInsert | consistent |
| T1 self | test count 12 | ok |
| T2 self | payments insert columns guessed | note carried (check \d) |
| T3 self | step 2 vacuous on empty test DB; verified via seed | ok |
| T5 self | expenses/tasks/payments columns guessed | note in plan |
| T6 self | create_hold call has fewer args than real signature | implementer adapts to real signature |
| T12 self | drop column cascade drops profiles policies | plan recreates update_self; must also keep review_author_read and select_self — check |
| T21 self | welcome hero test | ok |
| All DB tasks | `supabase db reset` wipes local dev data | backup taken (above) |

## Rulings

- Ruling: In T2, the six "no link" tables that apps/tests/triggers insert into (coupons, outbox, staff_shifts, leave_requests, attendance_records, tasks) get property_id backfilled and indexed but stay NULLABLE; T9 sets them NOT NULL after the functions and tests supply property_id. — Keeps the suite green at every commit; the spec's end state (NOT NULL) is reached in T9. — If wrong: a row could be written with null property_id between T2 and T9 on this feature branch only.
- Ruling: T5 may add property_id to existing test inserts that RLS now rejects, not only memberships. — RLS W(property_id) cannot pass with a null resort. — If wrong: none; T9 would have required it anyway.
- Ruling: T11's definer allow-list temporarily includes current_role, is_staff_or_above, is_admin, is_super_admin, assert_staff; T12 removes those five names from the list when it drops them. — The guard must pass at T11 and be strict after T12. — If wrong: guard is looser for one task on the branch.
- Ruling: T14 (not T18) deletes lib/features/admin/users_screen.dart, lib/data/repositories/user_admin_repository.dart, their tests and the /admin/users route. — They depend on UserRole, which T14 removes, and their RPCs are dropped in T10. — If wrong: no user-management screen between T14 and T18 on the branch.
- Ruling: `supabase db reset` is allowed for this plan. — Local dev data backed up to the file above. — If wrong: restore with psql from the backup.
- Ruling: T12 must keep profiles_select_self and profiles_review_author_read working after `drop column role cascade` (recreate any that the cascade drops). — Only update_self is named in the plan. — If wrong: users can't read their own profile.

## Progress
- Ruling: Pre-existing pgTAP failures in 01_profiles_test.sql (4) and 07_rls_test.sql (1), "infinite recursion detected in policy for relation profiles", are the baseline; Task 5 (which rewrites every profiles policy) must fix them and leave the whole suite green. Tasks 1–4 are accepted with only these 5 failing. — The recursion is in policies T5 replaces; fixing it twice is waste. — If wrong: T5 misses it and the final review catches a red suite.
- Ruling: Keep the function name `resort_role(uuid)` despite colliding with the enum `resort_role` (bare string literals parse as a cast); callers pass uuids or cast literals with ::uuid. — Spec names it; all real callers pass uuid columns/variables. — If wrong: a future bare-literal call silently casts; tests catch it.
- Task 1: minor (deferred): resort_role() function/enum name collision footgun — note for later SQL
- Task 1: complete (commits ee0e784..64a99a8, review clean)
- Task 2: minor (deferred): backfill order relies on VALUES row order (reservations before its dependents) — fails loudly if broken
- Task 2: minor (deferred): disable/enable trigger user relies on the migration running in one transaction
- Task 2: complete (commits 64a99a8..71146f9, review clean)
- Task 3: complete (commits 71146f9..241ee34, review clean)
- Task 4: fix round 1/5 (1 addressed, 0 open — 07_rls super_admin missing membership; commits a3c46cd..06064c2)
- Task 4: complete (commits 241ee34..06064c2, review clean)
- Ruling: Task 5 attaches the existing `fill_property_id('reservations','reservation_id')` trigger to `outbox` (it has reservation_id) in 0044, so trigger- and function-inserted outbox rows get their resort without rewriting enqueue functions early; Task 9's #34/#35 then only need to stop relying on it where reservation_id is null. — Keeps the suite green at Task 5 without weakening outbox_read or faking data in tests. — If wrong: an outbox row with null reservation_id stays null-resort (invisible to staff) until Task 9.
- Ruling: Accept Task 5's `grant select on resort_members to authenticated` (0043 omitted it; RLS still limits rows). — Required for any client read. — If wrong: none.
- Ruling: Accept public property read keeping `is_active` alongside `status = 'active'`. — Preserves the existing hide-a-property behaviour. — If wrong: none.
- Ruling: Task 5 adds a `before update` trigger on properties that raises P0008 when `status` changes and the caller is not a platform admin (`is_platform_admin()`), so resort owners/admins cannot suspend, archive or un-suspend their own resort. — Spec makes status a platform-admin action (set_resort_status); letting an owner flip it defeats suspension. — If wrong: owners can't self-archive, which the spec never offered.
- Ruling: Task 12 must rewrite the profiles self-update check (Task 5's version calls current_role(), which Task 12 drops) and must keep its new guard stopping users from granting themselves platform_admin. — Carried into Task 12 dispatch. — If wrong: self-escalation to platform admin.
- Ruling: Task 5 finding I1 (plan-mandated `tasks_update` assignee branch lets an assignee move a task to another resort) — fix: assignee branch also requires has_resort_role(property_id, true, Staff+) in using and with check; audit every other self/assignee/own branch on "no link" tables the same way. — Spec's core rule (no cross-resort writes) outranks the matrix text. — If wrong: none.
- Ruling: Task 5 finding I2 (plan-mandated `profiles_review_author_read` exposes full profile incl. phone of any reviewing guest across resorts) — fix: add `reviews.author_name text`, backfilled from profiles.full_name and set by a before-insert trigger; drop profiles_review_author_read; update lib/data/repositories/review_repository.dart `all()` to select `*` and the Review model to read author_name (with its test). — Spec's Global data rule forbids cross-resort guest profile reads; a denormalized display name keeps the reviews UI. — If wrong: a guest renaming themselves doesn't update old review names.
- Ruling: reviews read policy follows the spec's catalog rule (visible when the resort is active, plus the resort's staff and the author), not the brief's "unchanged true". — Spec is binding; brief contradicted it. — If wrong: reviews of suspended resorts hidden from guests browsing.
- Ruling: accept literal matrix for colleague profile reads (only owners/admins see the roster, so staff/accountants can't read colleagues' profiles). — Matches spec; revisit if staff screens need colleague names. — If wrong: some staff screen shows blank colleague names.
- Task 5: minor (deferred): outbox_templates platform-default branch accepts membership at an archived resort
- Task 5: minor (deferred): 20_tasks_test.sql ~168 stale comment ("explicit error")
- Task 5: minor (deferred): isolation test lacks cross-resort staff-write, anon active-B catalog, platform-admin-on-other-tables, second-resort profile cases
- Task 5: minor (deferred): properties_guard_status needn't be security definer
- Task 5: minor (deferred): reviews.author_name stores full name; app shows first name only — could store first name
- Task 5: fix round 1/5 (3 addressed, 0 open — tasks assignee cross-resort write; reviewer profile exposure; reviews active-only read; commits 3e65fd3..cbeb5ee)
- Task 5: complete (commits 06064c2..cbeb5ee, review clean)
- Ruling: Task 9 replaces the global `coupons.code` unique constraint with unique (property_id, code) when it sets coupons.property_id NOT NULL. — In SaaS, one resort's code must not block another's. — If wrong: none (strictly looser across resorts, same within one).
- Ruling: Accept P0020 (not P0008) for a customer acting on another's booking; the Flutter mapping of P0020 lands in Task 13. — Consistent single "not a member / not yours" code. — If wrong: between T6 and T13 the app shows a generic error for that rare case.
- Task 6: minor (deferred): confirm_booking checks resort status before the idempotent already-confirmed early return — a retried webhook after suspension gets P0022
- Task 6: minor (deferred): isolation test lacks staff/accountant role-boundary cases for compute_refund vs cancel_booking/block_dates, and A-admin confirm_booking on B hold
- Task 6: minor (deferred): block_dates unknown unit raises P0002 before role check (existence oracle; negligible)
- Task 6: complete (commits cbeb5ee..2a06000, review clean)
- Task 7: implemented (commits 2a06000..aed964d), review running
- Ruling: At the user's request for parallelism, app Tasks 13–21 run as a separate track in a Workflow on branch feat/resorthub-app (worktree /Users/wiljans/vsp/pasala_resorthub_app, from aed964d), concurrently with DB Tasks 7–12 on feat/resorthub-tenancy. Tasks 13→14 sequential; 15–21 in parallel, each in its own worktree/branch app-tN, then merged into feat/resorthub-app by one merge agent; the app branch merges into feat/resorthub-tenancy after Task 12. — App tests use fakes, not the DB; the DB tasks share one migration file and one Postgres so they stay sequential. — If wrong: merge conflicts in router.dart/app_shell.dart cost a merge agent's time; app code written against DB function names from the plan could drift from what Tasks 8–10 actually ship (checked at final review).
- Ruling: Task 9 also rewrites every storage.objects policy that uses the old global helpers (found: maintenance-photos bucket in 0035), scoping them to the resort (via the object path's reservation/issue id → property_id, or a resort-prefixed path), so Task 12 can drop is_staff_or_above() and staff can't read other resorts' photos. — Task 5's matrix covered only public tables. — If wrong: cross-resort photo reads, and Task 12's drop fails on the dependency.
- Task 7: minor (deferred): report_maintenance_issue not-found now P0002 instead of P0008 (normalization, undocumented)
- Task 7: minor (deferred): suspended-resort P0022 block duplicated in three guest functions (plan-mandated pattern)
- Task 7: fix round 1/5 (1 addressed, 0 open — accountant missing from Staff+ in 4 functions; commits aed964d..17e3af8)
- Task 7: complete (commits 2a06000..17e3af8, review clean)
- Task 8: ⚠️ Dart RPC callers not passing p_property_id — resolved: covered by app-track Tasks 15/16 (report, staff_performance, staff_shift repositories)
- Task 8: complete (commits 17e3af8..d6e6e7e, review clean)
- Ruling: Accept enqueue_outbox_message granted to authenticated with Staff+ check at the reservation's resort on direct calls (trigger/cron paths unchanged). — Plan's inventory and test assume it is callable; staff can only queue messages for their own resort's bookings. — If wrong: staff could re-send templated messages to their own guests.
- Ruling: Defer per-resort template overrides (outbox_templates `name` PK and unique (event, channel) are global, so a resort cannot yet shadow a default). Rendering never uses another resort's template. — No app surface edits templates in this sub-project; changing the key is a schema change better done with the template-editing feature. — If wrong: the spec's "resort row overrides default" isn't available until then.
- Ruling: Accept ical_import_event staying granted (since 0018) with an Admin+ check at the unit's resort. — Existing tests and behaviour rely on it. — If wrong: none (resort-scoped).
- Task 9: ⚠️ outbox_templates global name PK / unique(event,channel) lets one resort's admin claim a slot and probe names via 23505 — resolved: tracked with the deferred template-override ruling (no data leak)
- Task 9: minor (deferred): photo re-link — A staff can point an A issue's photo_url at a known object name of a guest's B photo (name unguessable); harden policy with not-exists-other-resort check
- Task 9: minor (deferred): photo isolation test is non-discriminating (passed pre-change); needs same-guest-two-resorts case
- Task 9: minor (deferred): NEW-resort checks in enforce triggers untested (RLS blocks first)
- Task 9: minor (deferred): ical_export_public returns 200/null for suspended resort — consider explicit error
- Task 9: minor (deferred): backfill has no guard for rows existing with empty properties
- Task 9: complete (commits d6e6e7e..38291f3, review clean)
- Task 10: ⚠️ app still calls list_profiles/set_user_role — resolved: app-track Task 14 deletes that screen/repository (ruling)
- Task 10: ⚠️ platform_resorts counts bookings by created_at and gross quote total — Ruling: accept for now (spec silent); revisit when the platform console (sub-project #3) defines metrics. — If wrong: dashboard numbers use a different definition than finance expects.
- Ruling: A platform-admin account may also hold resort memberships (e.g. VSP owning Pasala); the platform role itself grants no resort row access. — Real-world owner of Pasala runs the platform. — If wrong: spec intended strict separation; would need a check in create_resort/add_resort_member.
- Task 10: minor (deferred): owner authorization checked before the owner lock (TOCTOU window of ms)
- Task 10: minor (deferred): race tests cover self-demotion only, not mutual demotion / set-vs-remove
- Task 10: minor (deferred): dblink race fixtures can leak on mid-test error
- Task 10: minor (deferred): create_resort writes no audit_log row
- Task 10: minor (deferred): concurrent add/create can surface raw 23505 — map to P0005
- Task 10: complete (commits 38291f3..efb07d1, review clean)
- Session restart: Task 11 implementer and app workflow were interrupted. Task 11 re-dispatched to finish uncommitted work; app workflow resumed from run wf_b7a78ff7-4dc (T13/T14 cached; parallel implementers told to continue from partial worktrees).
- Second session restart (machine reboot; Docker restarted, supabase start). Task 11 agent resumed via SendMessage; app workflow resumed again from wf_b7a78ff7-4dc; keep-awake requested.
- Ruling: Accept reviews_set_author_name on the definer allow-list — it returns trigger, so it cannot be invoked as an RPC or outside a trigger, and reviews_insert RLS already pins customer_id = auth.uid(). — If wrong: none practical.
- Task 11: minor (deferred): reviews_set_author_name allow-list comment gives self-derived rationale instead of "trigger function"
- Task 11: complete (commits efb07d1..3f02c7a, review clean)
- App track (workflow wf_b7a78ff7-4dc): Tasks 13–21 all review-clean (T21 1 fix round); merged into feat/resorthub-app at b6f296d; flutter test 598/598, analyze no new issues. Deferred minors listed in app-track-minors.md.
- Task 13: complete (aed964d..f51a399, review clean)
- Task 14: complete (f51a399..a8b62b0, review clean)
- Tasks 15–21: complete (branches app-t15..app-t21 merged at b6f296d, review clean)
- Ruling: LOAD-BEARING gap from Task 14 minors — ProfileDirectoryRepository.listProfiles()/adminProfilesProvider (assignee pickers in 6 admin screens + staff performance) still call the `list_profiles` RPC that Task 10 dropped: runtime failure and, conceptually, cross-resort staff lists. Fix in the final fix wave: use list_resort_members(current resort) keyed by property id. — Spec's Review Focus #1. — If wrong: assignee pickers broken.
- Task 12: ⚠️ 0042→0046 migration on real data not run — resolved: Task 22 runs `supabase db reset --version 0042`, restores the local data backup, then `supabase migration up --local`
- Task 12: minor (deferred): 0046 recreates set_member_role only to change a comment (could edit 0045 comments instead)
- Task 12: minor (deferred): 07_rls_test.sql comments still say super_admin
- Task 12: complete (commits 3f02c7a..822cf5c, review clean)
- Task 22: complete (commits 7d7b6e9..3e55e6a) — real 0042 data migrated through 0046 successfully; flutter 598/598; pgTAP 726/729 with 3 pre-existing Asia/Kolkata-vs-UTC day-boundary failures (dashboard_summary / staff performance) that appear only in the ~5.5h window after IST midnight
- Ruling: LOAD-BEARING — 0043's "first property" pick (`order by created_at limit 1`, used for role→membership move and no-link backfills) is nondeterministic when properties share created_at (real local data: Riverside and Hilltop share it). Final fix wave changes every such pick to `order by created_at, id`. — Deterministic migration on any database. — If wrong: staff/rows attached to an arbitrary property.
- Ruling: The 3 time-of-day pgTAP failures are pre-existing (Kolkata vs UTC day boundary in dashboard_summary/staff_performance logic copied verbatim from 0038/0029), not introduced here; flag to the user rather than fix in this plan. — Out of tenancy scope; bodies were deliberately copied unchanged. — If wrong: they're caused by this branch and should block merge (final reviewer to confirm).
- Final review (opus): verdict "with fixes" — C1 list_profiles, C2 reviews unscoped, C3 guest cross-resort food/activity writes, C4 nondeterministic first-property, I1 archived-membership loop, I2 URL-trusting admin routes; plus cheap F1–F5. Dispatched as one fix wave (final-fix-findings.md).
- Ruling: Welcome-screen hero strip (spec requirement, Task 21) — controller verifies it live in the browser pane after the fix wave rather than the fix subagent. — Subagents have no browser. — If wrong: strip still visible.
- Ruling: Defer (not in fix wave): reviews.author_name full vs first name; assignee-membership check triggers; suspended-resort resort-name display for guests; platform-admin link back to own resort; admin-only cross-resort FK consistency (coupon_redemptions.coupon_id, rate_rules/reservations.slot_type_id); plus every item the final reviewer classed "fine to defer". — Not merge-blocking per final review. — If wrong: follow-up tasks needed.
- Ruling: The 3 time-of-day pgTAP failures are confirmed pre-existing by the final reviewer; not fixed here; flagged to user.
- Final fix wave: 3e55e6a..190d6c3 (8 commits); flutter 622/622; pgTAP 731/734 (3 known IST-window); real data restored; /reviews → /property/:id/reviews; archived resorts show no platform action
- Final fix wave re-review (opus): all 11 findings ADDRESSED, no new Critical/Important. Minors: owner self-removal silently switches to their other resort; no route-level test that /admin/rates|block|ota are wrapped in ResortUnitGuard; bookmarked /reviews now 404s.
- Controller browser check (run-web-resorthub, 400x860 and desktop): welcome shows ResortHub branding; left-edge hero strip gone. Minor (deferred): ~4px darker band at the right edge of the hero at phone width.

---

# App track deferred minors (from workflow wf_b7a78ff7-4dc)

## Task 13
- ⚠️ Cannot fully verify from this diff: constraints.md's Review Focus item 4 ("A remembered current resort the user has since been removed from must be discarded at sign-in, not used") is labeled "Owning task: 13," but neither task-13-brief.md nor this diff implements any `currentResortProvider` or remembered-resort logic — that provider does not exist anywhere in the repo yet (confirmed via `grep -rn currentResort lib/`, only hits are the forward-pointing `@Deprecated('Use memberships and currentResortProvider')` strings this task added in lib/data/models/app_user.dart:3,6,19,37,50,56,59). The design spec (docs/superpowers/specs/2026-09-24-resorthub-tenancy-design.md:239-242) describes `currentResortProvider` as a separate piece (shared_preferences-backed, paired with the routing changes at lines 247-262), which strongly suggests it belongs to a later task (likely the one the deprecation messages point to, given 'Task 14 removes' the legacy role fields in the same design section). Recommend the controller confirm the task that builds `currentResortProvider` explicitly implements and tests the discard-on-invalid-resort behavior — constraints.md's 'Owning task: 13' should not be read as satisfied by this diff.
- lib/data/repositories/auth_repository.dart:16 — the new `.select('*, resort_members(property_id, role, properties(name, status))')` line is 81 chars, one over the project's 80-col width; cosmetic only, and the constraint explicitly forbids running `dart format` over whole files/dirs so this is not blocking.
- test/data/app_user_test.dart:6-7,16 also exceed 80 chars, but these lines were copied verbatim from task-13-brief.md's own test snippet, not an implementer choice.
- lib/data/models/resort_membership.dart:39-45 (ResortMembership.fromJson) silently defaults resortName to '' and status to 'active' when the joined `properties` map is null/absent, with no test exercising that branch (only the 'unknown role throws' edge case is tested, per the brief). Low risk since `_profileFor`'s query always requests the join, but worth a defensive test or comment if a resort_members row can ever arrive without its properties row.
- Self-flagged by the implementer in task-13-report.md 'Concerns': `flutter analyze` on this checkout shows 2 pre-existing infos, not the '3 pre-existing infos' constraints.md documents. Harmless (fewer, not more) and doesn't affect this task, but constraints.md's baseline note is stale and worth a one-line fix for future tasks that compare against it.

## Task 14
- lib/data/repositories/profile_directory_repository.dart:23-32 — `listProfiles()` wraps its call in an inline try/catch instead of the codebase's established `_guard<T>` helper (constraints.md: "repositories wrap calls in `_guard` which maps errors through `mapPostgrestError` ... Keep it"; see e.g. `refund_rule_repository.dart:15-20` for the pattern, used even by single/few-method repos). Functionally identical today (same `mapPostgrestError` mapping), but deviates from the stated convention for a newly-created file. Wrap it in a private `_guard` for consistency with every other repository added in this diff and the rest of the codebase.
- lib/core/current_resort.dart:38-41 — `loadCurrentResortFor` (a new public function, not in the brief's literal interface list but needed because `landingPathFor`'s signature grew a `resort` argument) has no direct unit test; it thinly wraps the well-tested `resolveCurrentResort` plus a `SharedPreferences` read, so risk is low, but a one-line test (mock prefs value -> expected membership) would close the gap.
- lib/features/auth/login_screen.dart:43 and lib/features/auth/signup_screen.dart:47 — `loadCurrentResortFor(user)` reads `shared_preferences` directly and computes the initial `context.go(...)` target independently of `currentResortProvider`'s own async `_loadRemembered()` load (current_resort.dart:78-86). If that provider's background load hasn't resolved yet at the moment GoRouter's `redirect` re-runs right after this navigation, a multi-resort user with a remembered pick could be routed to `/choose-resort` for one frame before `currentResortProvider` catches up and the router redirects them onward — a self-healing race, not covered by any test. Not blocking, but worth a note for whoever next touches sign-in/sign-up navigation.
- Self-flagged by the implementer and confirmed by inspection (lib/data/repositories/profile_directory_repository.dart:17-18, lib/features/owner/staff_performance_screen.dart and the 6 admin assignee-picker screens): `adminProfilesProvider`/`ProfileDirectoryRepository.listProfiles()` still calls the pre-tenancy `list_profiles` RPC, unscoped to the current resort, so once multiple real resorts exist every assignee picker will offer staff from every resort (a Review Focus #1-shaped gap). Constraints.md assigns Review Focus #1 to Tasks 15-17 and this task touches no DB migrations, so this is pre-existing (unchanged) behavior, not a regression introduced here — flagging only so the controller can confirm Task 18 (`list_resort_members`) is expected to close it, since Task 18's own brief doesn't mention these 7 screens.

## Task 15
- test/features/reports/reports_screen_test.dart:2708-2717 — the new 'Review Focus #1' test for ReportsScreen only asserts `revenueFilters` are scoped to the current resort; `occupancyFilters` (same ReportFilter, same currentResortProvider read) is never asserted. Low risk since both filters are built from the same `filter` value in reports_screen.dart:184/232, but worth a one-line addition for symmetry with the sibling tests in dashboard_screen_test.dart and staff_performance_screen_test.dart.
- Report's stated flutter analyze baseline (2 pre-existing infos in lib/features/stay/service_request_screen.dart) differs from constraints.md's stated baseline of '3 pre-existing infos'. Both infos are in a file this task never touches, so it doesn't block this task, but the controller may want to reconcile the baseline count against the actual repo state.
- lib/features/owner/expenses_screen.dart:287 and lib/features/owner/food_sales_screen.dart:323 call `ref.invalidate(expensesProvider)` / `ref.invalidate(foodSalesProvider)` with no filter argument (invalidating the whole family) after create/update, rather than the specific `filter` already in scope (as `_delete` does two methods up). This is pre-existing code untouched by this diff, valid Riverpod usage, and not a task-15 defect — flagging only for awareness since it's adjacent to lines this task did touch.

## Task 16
- test/features/admin/staff_shifts_screen_test.dart:3002 (FakeStaffShiftRepository.createdPropertyIds) — populated in createRange but never asserted on anywhere in the file, because createRange's call site can't be driven through the OS date-range picker in a widget test. Self-flagged by the implementer's report as a harmless loose end; true but worth trimming or asserting on directly (the existing plain `test(...)` at staff_shifts_screen_test.dart:171 already calls repo.createRange directly and could add `expect(repo.createdPropertyIds, ['p1'])`).
- lib/features/staff/my_food_orders_screen.dart:1746, lib/features/staff/my_maintenance_issues_screen.dart:1823, lib/features/staff/my_service_requests_screen.dart:1866 — all three correctly call ref.watch(currentResortProvider)!.propertyId (verified by reading the diff), but none of the three has any widget test file, before or after this change, so the screen-level wiring of the resort id (as opposed to the repository-level filter, which the corresponding admin screens do cover with a two-resort isolation test) is unverified by the suite. Pre-existing gap, not introduced by this task, and already surfaced in the implementer's own report under 'Concerns' — flagging per Review Focus #1 being explicitly owned by task 16, but not a blocker given the repository behavior itself is proven elsewhere and the code was verified by reading.

## Task 17
- lib/features/admin/admin_bookings_screen_test.dart:19-33 (and duplicated verbatim in test/features/admin/admin_home_screen_test.dart and test/features/admin/property_form_test.dart): the `_FixedCurrentResort extends CurrentResort` override class is copy-pasted into all three new/changed test files. This matches an already-established repo-wide convention (identical pattern exists in test/features/owner/expenses_screen_test.dart, food_sales_screen_test.dart, shell/app_shell_test.dart, staff/staff_dashboard_hub_screen_test.dart, staff/staff_profile_screen_test.dart under names like `_FixedResort`), so it is not new tech debt introduced by this task, but a shared `test/support/fixed_current_resort.dart` helper would remove the duplication repo-wide. Not blocking for this task.
- lib/features/admin/property_form_screen.dart:234: `AppBar(title: const Text('Properties'))` is pre-existing (unchanged context line in this diff) and doesn't match the design constraint that UI copy should say "Resort". Out of scope for this task's brief (which only asks for resort-scoping, not a copy pass) and not introduced by this diff, but flagging for whichever task owns the copy audit.

## Task 18
- lib/features/owner/team_screen.dart:392-456 — `_MemberTileState._changeRole` and `_remove` duplicate the same busy-flag/try-catch(BookingFailure)/snackbar/finally scaffolding almost verbatim (differing only in the RPC call and remove's extra confirm dialog). Not severe enough to block, but a small `_run(Future<void> Function() action)` helper would remove the duplication.
- flutter analyze baseline mismatch: constraints.md states the project baseline is '3 pre-existing infos, no new ones,' but the report's analyze run shows only 2 (both pre-existing, in service_request_screen.dart, unrelated to this task's files). Not verifiable from the diff alone since analyze output isn't part of it — likely explained by baseline drift across the plan's sequential tasks, but worth a quick controller sanity check that no info was silently dropped rather than fixed.

## Task 19
- lib/features/platform/platform_screen.dart:317-323 — surfaces bookings365d/revenue365d as a second line per row, beyond the brief's literal 'Screen: ... 30-day bookings and revenue' bullet. Report flags this itself as a judgment call; it's a reasonable read of the ResortSummary interface (which already commits to fetching those fields) and matches the design spec's platform_resorts() description, so not a real Missing/Extra issue — just noting for the controller in case the brief author wanted the screen literally minimal.
- lib/data/repositories/platform_repository.dart:157-164 — createResort casts the create_resort RPC's scalar return directly to String with no null/type guard, mirroring the established rotate_ical_token pattern (ical_repository.dart:123) elsewhere in the codebase, so this is consistent with existing style rather than a new risk.

## Task 20
- lib/features/account/my_bookings_screen.dart:135-141 and lib/features/stay/my_stay_screen.dart:153-159 duplicate the identical `if (reservation.resortName case final resortName?) Text(resortName, style: textTheme.labelLarge?.copyWith(color: scheme.onSurfaceVariant))` block verbatim across two files. Small enough not to block, but worth extracting into a shared `_ResortLabel` widget so the two don't drift.
- lib/features/stay/my_stay_screen.dart:153-159 (new resort-name display in `_Hub`) has no dedicated widget test asserting the name renders when `reservation.resortName` is set -- test/features/stay/my_stay_screen_test.dart was not touched by this diff and its existing cases never pass a `resortName`. The brief's file list does name my_stay_screen.dart as in-scope, but its Test section only calls out browse_screen_test.dart and my_bookings_screen_test.dart, so this isn't a missed listed-file requirement, just an added behavior that shipped without its own RED/GREEN test cycle. The code path is trivial and mirrors the already-tested booking-tile pattern, so low risk.
- test/features/browse/browse_screen_test.dart:157-163 ('lists every active resort -- both cards render -- with two properties') duplicates assertions already covered by the pre-existing 'shows a hero header above the property list' test (same file, uses the same two-property fixture and checks the same two texts). Harmless and explicitly requested by the brief's test list, but adds no new coverage beyond what already existed pre-diff.
- Not a diff defect, but worth the controller's awareness: `properties_read` RLS (0044_resort_policies.sql:27-30) only lets a non-member read a property when it is `status = 'active'`. A guest's booking at a suspended resort (Review Focus #5, owned by tasks 5/7/12, not 20) will still load via `reservations` RLS, but the `properties(name)` embed will resolve to null for that row, so My Bookings/My Stay will silently show no resort label for a suspended resort's booking -- degrades gracefully (nullable field, no crash) but is a minor UX gap outside this task's owning scope. Named as a risk and checked against 0044_resort_policies.sql; not the responsibility of task 20 to fix.

## Task 21
- web/index.html:26 — `<meta name="apple-mobile-web-app-title" content="pasala">` was left unchanged while `<title>` was rebranded to ResortHub (web/index.html:31), leaving inconsistent branding for iOS home-screen bookmarks. Not explicitly named in the brief's file list but is the same class of change.
- task-21-report.md lines 119-136 — the whole-repo `flutter test` run did not finish/was not confirmed clean before commit (attributed to shared-machine contention). Only the focused widget-test file and `flutter analyze` are confirmed. Low risk given the change's small surface, but worth a follow-up confirmation before merge per the implementer's own recommendation.
- Finding 1's underlying defect — the brief's Step 3 requirement to actually 'find the cause of the duplicated left strip ... and fix it' — remains unfixed and unverified; this was explicitly acknowledged by the finding as unclosable from this environment (no app/server start permitted per this track's override), and the finding's own prescribed remedy was only to flag it rather than fix it. Controller should schedule a live-browser/CanvasKit or golden-image check of the welcome screen hero at 400px before treating Task 21 as fully complete — this is already captured as an OPEN ITEM comment in test/features/auth/welcome_screen_test.dart:48-70 and in task-21-report.md's 'Fixes -- round 1' section, so no further action is needed from this review round.

